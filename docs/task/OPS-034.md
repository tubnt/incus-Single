# OPS-034 QA-008 旅途审查 bug 集中修

- **status**: completed
- **completedAt**: 2026-05-03
- **priority**: P1
- **owner**: claude
- **createdAt**: 2026-05-03
- **scope**: 用户 `继续` —— 修 QA-008 报告的全部 11 条 bug

## 修复清单

| ID | 严重度 | 文件 | 改动 |
|---|---|---|---|
| **A1** | P1 | `web/src/app/routes/index.tsx` | dashboard "新建云主机" `/billing` → `/launch`（2 处：command + Link） |
| **A9** | P1 | `web/public/locales/{zh,en}/common.json` | 补回 `admin.users.title` 键（OPS-032 误删） |
| **A2** | P2 | `web/src/app/routes/vm-detail.tsx` | 4 个 InfoCard label 走 i18n |
| **A6** | P2 | `web/public/locales/zh/common.json` | `admin.amount` / `invoice.order` 翻译；补 `invoice.amount` / `invoice.status` |
| **A8** | P2 | `web/src/app/routes/admin/clusters.tsx` | 6 个表头走 i18n |
| **A10** | P2 | `web/src/app/routes/admin/ha.tsx` | 3 stat cards label + 状态文案映射 + 列头 i18n |
| **A3** | P3 | `web/public/locales/zh/common.json` | `snapshot.create` "快照" → "新建快照" |
| **A7** | P3 | `web/src/app/routes/admin.tsx` 或 `routes/admin/index.tsx` | 加 redirect `/admin` → `/admin/monitoring` |
| **A11** | P3 | `web/src/app/routes/launch.tsx` | mobile summary card 改隐藏（`md:flex hidden`），主操作走 mobile bottom bar |
| **A5** | P3 | `web/src/app/routes/__root.tsx` | SSO `rd` 用当前 location.pathname + search |

## 范围外（单独评估）

- **A4** CJK 字体自托管 — 需要选 Noto Sans SC subset 方案 + 工具链 + ~1.5 MB asset。涉及决策：要不要把 SC subset 打入 deploy bundle，建议你单独决策。

## 验收

- [x] `bun run typecheck` PASS
- [x] `bun run lint` 0 errors / 11 warnings（baseline 不变）
- [x] `bun run build` PASS · dist 同步 · `go build` PASS
- [x] 部署到生产：`a9961dff9b591937` linux/amd64，systemd active
- [x] curl 抽查 i18n 键全部生效：
  - `admin.amount: 金额` / `invoice.order: 订单` / `invoice.amount: 金额` / `invoice.status: 状态`
  - `snapshot.create: 新建快照`
  - `ha.statHaStatus: HA 状态`
  - `admin.nodes.colName: 节点`
- [x] 备份：`/usr/local/bin/incus-admin.bak.20260503_062830`

## A9 修订

实测发现 BUG-A9 是误判：`admin.users.title` 在 zh + en 两边都存在。QA-008 截图里看到的 "□□ (2)" 是 BUG-A4 字体 tofu 渲染的"用户 (2)"，**不是 i18n key 缺失**。无需修复。

## 回滚

```bash
mv /usr/local/bin/incus-admin.bak.20260503_062830 /usr/local/bin/incus-admin && systemctl restart incus-admin.service
```
