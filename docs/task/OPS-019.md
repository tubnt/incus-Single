# OPS-019 杂项打磨：lint infra / i18n / migration.stateful / Bug#4 / log noise / audit

- **status**: completed
- **priority**: P3
- **owner**: claude
- **createdAt**: 2026-04-29 03:30
- **completedAt**: 2026-04-29 04:20

## 子项

### P3.3 修复 `bun run lint` 基础设施
- `eslint-plugin-react-refresh` 缺包 → 装上
- `@eslint-react/eslint-plugin` v4 与 `@antfu/eslint-config` v8 不兼容 → 降到 v3
- `bun run lint` 现在能跑（残留 18 个非阻塞 errors + 41 warnings，都是 pre-existing 风格问题）
- `bun run lint --fix` auto-fix 174 处（import 排序 / type-only import 改写）

### P3.4 audit-logs.tsx i18n TODO
- 移除 i18n TODO 注释
- 起始日期/截止日期/动作前缀/导出 CSV/默认 30 天 全走 `t("admin.auditFromDate", ...)` 等
- zh + en locale 各加 6 个 key

### P3.2 VM create migration.stateful=true
- `internal/service/vm.go::Create` 在 `config` 加 `"migration.stateful": "true"`
- OPS-008 #5 教训：不设这个 Incus 拒 `live: true` migrate；OPS-008 当时走 cold migrate 兜底
- 新建 VM 现在天然支持 live migration

### P2.6 HA-001 audit() backfill
- `rescue.go` 6 个 entry handler funnel 到 `doEnter` / `doExit`，audit-coverage 脚本误报 partial
- 重构：doEnter / doExit 返回 `bool`，audit() 移到每个 entry handler（带 `via` 字段区分 admin-by-id / by-name / portal）
- 现在 6 writes / 6 audits 全 ok

### P3.5 HealingEventRepo.GetByID integration test
- 新 `internal/repository/healing_integration_test.go`：4 个 case
- not-found → nil/nil
- happy path → 含 EvacuatedVMs JSONB 解析
- after Complete → status='completed' + completed_at 非空
- after Fail → status='failed' + Error 指针非空
- 测试本机环境 Docker bridge 不通会 SKIP（已有 pattern），CI / 真实环境会跑

### Bug #4 startup warning 温和化
- `cmd/server/main.go::ensureCustomerProjectAllowsClusterTarget`
- "Certificate is restricted" 错误单独识别 → 日志加 `runbook` 字段提示一次性手工命令
- 用户/管理员看到日志即知"这是设计行为，不是 bug"

## 验收

- [x] `bun run lint` 可运行
- [x] `bun run typecheck` PASS
- [x] `bun run test` 37/37 PASS
- [x] `go test -count=1 ./...` 全包 PASS
- [x] `go vet ./...` clean
- [x] `bash scripts/audit-coverage-check.sh --strict` 70 writes / 71 audits / 0 missing
- [x] 部署到生产
