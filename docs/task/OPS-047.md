# OPS-047 PLAN-051 Follow-up —— 大块项推迟单（saga / N+1 / 性能 Phase 2/3）

- **status**: backlog
- **priority**: P1
- **owner**: TBD
- **createdAt**: 2026-05-10
- **scope**: 把 PLAN-051 一次会话内未消化的~50 项分类为可独立交付的子任务，
  每个子任务独立 PR；按"风险高 / 影响多 / 需要独立设计"剔出。本任务作为 backlog
  入口，不在本任务直接动手。

## 上游

PLAN-051 已落地 24 项最高影响项（见 docs/changelog.md 2026-05-10 条目）。
本任务追踪剩余 ~50 项；剩余项的共同特征：

- 单项改动 ≥ 100 行；或
- 涉及 DB schema 新表；或
- 单项 e2e 配套不可省（如 invalidate 缩窄）；或
- 是性能 / refactor 维度，不修也能用

## 子任务清单

### A. 资金路径正确性（独立 RFC + e2e）

- **F-06** Pay saga：`OrderHandler.Pay` 多步写入原子化。当前 6 步独立 SQL 调用，
  任一段崩溃留下孤儿订单。设计选项已记录在 PLAN-051 决策 D-03 = B。
- **F-25** Order FSM transition table：`UpdateStatus` admin 端任意流转 → 副作用
  漂移
- **F-66** 后端 `POST /admin/users:batch-topup` 原子端点 + 前端
  `useBatchTopUpMutation` 包装

### B. 防火墙重构

- **F-22** 批量绑/解绑改 jobs runtime（参考 `vm_migrate_batch`）
- ✅ **F-20** ListGroups N+1 → 单 SQL JOIN（2026-05-10 done：`ListRulesByGroups`）
- **F-41 + F-42** EnsureACL + stop/patch/start 写 `firewall_apply_ops` +
  sweeper（新表 + 新 worker）

### C. Node-join 路径

- **F-33** `cluster_node_add.onLine` 500ms tick 批量 flush（避免 PG 连接池打爆）
- ✅ **F-34** `requestJoinToken` async 分支真正解析 op metadata 取 token
  （2026-05-10 done）
- ✅ **F-62/F-63/F-64** wizard 跨 stage 回退状态清理（2026-05-10 done：
  debounce 300ms / 删 computeHeuristics / stage/back 清子状态）

### D. TanStack Query invalidate 缩窄（13 处）

- ✅ **F-50** `web/src/features/vms/api.ts` 10/13 处缩窄到具体 key
  （2026-05-10 done；剩余 2 处 rescue by-name 因无 cluster 上下文保留 vmKeys.all）
- ✅ **F-54 + F-56** `useEnableStatefulBatchMutation` /
  `useMigrateBatchMutation` 加 `clusterList` invalidate（2026-05-10 done）
- 每处配 e2e；保留 `staleTime: 10_000` 兜底

### E. Auth / 密钥分离

- **W4** 关键端点独立限流 + X-Forwarded-For + `TRUSTED_PROXIES` env
- ✅ **W5** Incus REST path-segment URL escape sweep（2026-05-10 done：
  client.go / vm.go / snapshot.go / clustermgmt.go ~20 处全部 url.PathEscape +
  url.QueryEscape）
- **O3** step-up PKCE（需 server-side state→verifier map，独立设计）
- ✅ **O6** cloud-init 模板 yaml 解析校验（2026-05-10 done：
  `validateCloudInitTemplate` 走 yaml.v3 Unmarshal）
- **O1** VM 密码 token 化（决策 D-09 = B：当前直返 + Cache-Control: no-store）

### F. Memory / Cache GC

- ✅ **F-13** `aiRate` 后台 ticker 清理（2026-05-10 done）
- ✅ **F-14** `probeCache` 后台 ticker 清理（2026-05-10 done）
- ✅ **F-7** `vmListCache` 加 5min TTL（2026-05-10 done）
- ✅ **F-46 + F-47** ListPaged 单 SQL `COUNT(*) OVER ()` + IPsByNames chunk
  1000（2026-05-10 done；pgx stdlib 不支持 pq.Array，保留 placeholder 列表）

### G. i18n / Error UX 全量替换（QA-009 §2-I）

- ✅ **N-04** toast.error 余下约 30 处 formatError 接入（2026-05-10 done）
- ✅ **N-05** AlertDescription 8 处 formatError 接入（2026-05-10 done）
- **N-06** VM/Node/IP 状态字符串 enum 归一 + formatXxxStatus 全用
- ✅ **N-07** 表单 maxLength：products(name 64 / slug 32) / os-templates(name 64
  / slug 32 / source 128) / ssh-keys(name 64)（2026-05-10 done）。**剩余**：
  firewall description / ticket reply textarea
- ✅ **N-08** mutation 前 trim：admin/firewall 创建组 / admin+portal tickets
  reply（2026-05-10 done）
- ✅ **N-09** firewall rule pattern 校验：CIDR / port-range（2026-05-10 done）
- ✅ **N-10** topup customAmount `max=$100k`（2026-05-10 done）
- ✅ **N-11** stateful banner 失败重置（2026-05-10 done：statefulLastError state）
- **N-12** `tickets.$id.tsx` path-param 子路由（决策 D-21 待用户回答邮件格式）
- ✅ **N-13** node-join reducer back action 清子状态（2026-05-10 done）
- ✅ **N-14** firewall 默认组全删 window.confirm 二次确认（2026-05-10 done）

### H. 性能 Session-3 Phase 2/3

- ✅ **🔴-3** CommandPalette + Sonner lazy load（2026-05-10 done：
  React.lazy + Suspense；CommandPalette 仅 commandOpen=true 才挂载）
- **🟡-1** fontsource @font-face 抽出独立 stylesheet（决策 D-17 = C：仅抽
  Inter / JetBrains Mono）
- **🟡-5** i18n zh inline 到 entry chunk + Suspense 等 ready
- **🟡-2** modulepreload 实测命中率 < 50% 的链接降级 prefetch
- **🟡-4** DataTable 接 `@tanstack/react-virtual` 阈值 100 行 + DropdownMenu
  lazy mount
- **🟡-3** ws-aware 轮询自适应间隔
- **🟡-M1** admin/vms + admin/users mobile 卡片视图（决策 D-20 = B）
- **🔵-5** xterm WS 与 Terminal 实例化并行

### I. 杂项

- **F-39** runtime.runOne defer cred.Wipe 集中
- **F-45** decryptOnRead 返 sentinel error 让 admin 区分"未设" vs "密钥轮换坏了"
- **F-40** broker RWMutex
- ❌ **F-67** `useTestSSHMutation` / `useExecSSHMutation`：复查发现
  `admin/node-ops.tsx:25-26` 在用，**非 dead code**，agent 误报，剔出 backlog
- **F-55** downloadClusterEnvScript AbortController + revoke blob URL
- **F-61** `useGlobalShortcut(key, handler, opts)` helper 抽出
- ✅ **F-30** `writeJSON` Encode 错误打 slog warn（2026-05-10 done）
- **F-69** DataTable columns 数组外置避免语言切换重 shape
- **F-70** TrashBanner 拆 Countdown 子组件
- **F-68** cluster-vms-table selectedNames 用 Set
- **F-58 / F-66** 加 ESLint custom rule 禁 `(err as Error).message` 直显（决策 D-24 = A）
- ✅ **N-18** favicon 内联 SVG（2026-05-10 done）
- ✅ **N-19** SSH key 名 maxLength=64（2026-05-10 done）
- ✅ **N-20** firewall warning 模板 i18n key 表（2026-05-10 done）
- ✅ **N-21** MigrateBatchSheet 加预估总耗时（2026-05-10 done）

### J. 运维脚本

- **F-03 后续** `cluster/scripts/join-node.sh` 与 embedded 副本对齐 + ci gate
  `diff -q`（PLAN-051 §2-A 已识别但工作量大；推迟）
- **3.3 / 3.5 / 3.7 / 3.9** join-node.sh / create-vm.sh 杂项 hardening

## 实施建议

按 PLAN-051 §3 的依赖顺序：
1. **D / G**：低风险高频价值，每周可消化几条
2. **A / B / C**：需 RFC 单独立项
3. **E / I / J**：每月 2-3 项
4. **F / H**：性能监控触发后再做

## 关联

- 上游：PLAN-051
- 待用户回答：决策 D-21（邮件 ticket 链接格式）
