# OPS-002 新 IP 段 202.151.179.0/26 VLAN 376 接入（PLAN-021 Phase F）

- **status**: completed
- **priority**: P1
- **owner**: claude
- **createdAt**: 2026-04-24 14:00
- **updatedAt**: 2026-04-24 14:40
- **completedAt**: 2026-04-24
- **relatedPlan**: PLAN-021（Phase F）

## 描述

把新扩容的公网段 `202.151.179.0/26` VLAN 376 gw `.62` 接入 IncusAdmin。物理层已 trunk（node1 ping .62 通）；本 task 只做数据接入 + 多池支持。

## 交付

- migration 010 新增 `ip_pools(/26)` + 52 条 `.10-.61` 预填
- config 新增 `CLUSTER_IP_POOLS_JSON` env（JSON 数组），legacy 单池 env 保留 back-compat
- `allocateIP` 池间 fallback（/26 满 → /27 继续）
- 单测 9 case（config 4 + allocator 5）
- 生产部署：env 切换 + 服务重启无 error；`/admin/ip-pools` 返 2 pools 正确；AllocateNext SQL 模拟走 /26 选中 `.10`

## 依赖

- **blocked by**: 无
- **blocks**: 无（PLAN-021 Phase C/D/E/G 独立）

## 笔记

- Seed 保守：保留 `.1-.9` 给网络基建；net/broadcast/gateway 自然排除
- 池顺序 /26 primary / /27 fallback（新 VM 先吃新段，旧段保留做溢出兜底）
- 真 VM 未创建（避免破坏生产 vm-a3e86d）；后续第二台 VM 创建时可自然验证
