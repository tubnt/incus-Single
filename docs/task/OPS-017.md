# OPS-017 Firewall ingress/egress 双向规则

- **status**: completed
- **priority**: P2
- **owner**: claude
- **createdAt**: 2026-04-29 04:15
- **completedAt**: 2026-04-29 04:20
- **relatedTask**: OPS-004（Phase E firewall 仅 ingress；本任务把规则方向化）

## 修复

### Migration 014_firewall_rule_direction.sql
- `firewall_rules` 加 `direction TEXT NOT NULL DEFAULT 'ingress'`
- CHECK 约束：`direction IN ('ingress','egress')`

### `internal/model/models.go`
- `FirewallRule.Direction string` 字段

### `internal/repository/firewall.go`
- scan / Insert / ReplaceRules 全部覆盖 direction
- 兼容历史行：`COALESCE(direction, 'ingress')`

### `internal/service/firewall.go::EnsureACL`
- 新 `splitRulesByDirection` helper，把 flat 列表拆 ingress / egress
- Incus ACL body 现在两个槽都可填

### `internal/handler/portal/firewall.go::firewallRuleBody`
- `Direction string \`json:"direction" validate:"omitempty,oneof=ingress egress"\``
- `rulesFromBody` 缺省回填 "ingress"（向后兼容）

### Frontend `web/src/features/firewall/api.ts`
- `FirewallRule.direction?: "ingress" | "egress"` 可选字段

## 验收

- [x] `go build ./...` PASS
- [x] `go test ./...` 全包 PASS（含 service / handler / repository）
- [x] `go vet ./...` clean
- [x] `bash scripts/audit-coverage-check.sh --strict` 70 writes / 71 audits / 0 missing
- [x] vitest 37/37 PASS
- [x] frontend typecheck + build PASS（`index-*.js` 包含可选 direction 字段）
- [x] migration 014 已部署到生产（`ALTER TABLE` + CHECK 应用 OK）

## UI（留作渐进改进）

`/admin/firewall` 的规则编辑器目前只有 ingress 输入；下一步加 direction 下拉选择器（`Ingress` / `Egress`）。后端 / API / DB 都已就绪，前端只要加一个 `<select>` 即可。

## 笔记

- 与 OPS-016 同批部署
- Egress 在 Incus ACL 是 first-class，service 层用 splitRulesByDirection 一次性拆分
- 完全向后兼容：所有历史行 + 不传 direction 的 API 调用 → 默认 ingress
