# QA-003 Fix 15 QA bugs from 2026-04-16 production browser testing

- **status**: completed
- **priority**: P1
- **owner**: claude
- **createdAt**: 2026-04-16 23:35
- **completedAt**: 2026-04-17 00:40

## Description

Fix 15 actionable bugs found by comprehensive production QA (tmp/QA-REPORT-2026-04-16.md).
No P0 issues; B17 is the highest user-visible risk (mobile unusable).

Frontend (12): B17 mobile sidebar, B10 console route param, B14 nonexistent VM detail,
B9 default project, B16 Add Cluster validation, B6 currency symbols, B5/B11/B12 i18n coverage,
B1 storage pool type label, B13 delete confirm dialog, B15 node-ops IP validation.

Backend (2): B8 admin VM list missing IP (merge DB IPs into cluster API response),
B7 audit log target_id all 0 for resources with real numeric IDs.

Out of scope (documented in the plan): B2/B3/B4 require OAuth2 Proxy / reverse-proxy config,
not application code.

See `docs/plan/PLAN-010.md` for full context and fix list.

## ActiveForm

Fixing QA-003 bugs

## Dependencies

- **blocked by**: (none)
- **blocks**: (none)

## Notes

Deployed to production 2026-04-17 00:34 UTC (vmc.5ok.co).

Browser-verified in production: B1, B5, B6, B7 (VM path), B10, B11, B13 (evacuate),
B14 (admin + portal — initial portal stuck on Loading when id missing, supplemental
fix applied and redeployed), B15, B16. B17 code-verified (md:hidden header toggle +
-translate-x-full sidebar) — physical viewport test skipped (no resize tool).

Awaiting VM data to visually confirm in browser: B8 (admin VM IP column),
B9 (VM detail default project), B13 for VM/snapshot delete specifically.
Code path and unit-level wiring are in place.

Deferred (tracked separately):

- B2 /oauth2/callback 500 — OAuth2 Proxy config
- B3 missing security headers — reverse-proxy responsibility
- B4 /favicon.ico 403 — OAuth2 Proxy skip_auth list
