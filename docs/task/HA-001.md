# HA-001 事件驱动 HA 实现 + 故障演练 + 历史回放

- **status**: completed
- **priority**: P1
- **owner**: claude
- **createdAt**: 2026-04-19 01:15
- **updatedAt**: 2026-04-24 00:00
- **completedAt**: 2026-04-24
- **relatedPlan**: PLAN-020（Phase C-H；Phase A-B 由 INFRA-006 承担）

## 描述

实现 PLAN-020 的 HA 真正化部分（Phase C-H），把"纸面 HA"改造为事件驱动的可观测系统。与 INFRA-006（轮询 reconciler + gone 状态）配合，共同组成 PLAN-020 的完整交付。

### 本 task 范围（PLAN-020 Phase C-H）

1. **Phase C 事件流订阅**：`internal/worker/event_listener.go`，每集群 WebSocket 连 `/1.0/events`，断线重连 + 重连后全量对账 + 事件幂等（LRU jti）
2. **Phase D healing_events 表**：migration 008 + 手动 evacuate 改造 + 节点 offline → 自动记录 → evacuate 完成/超时状态机
3. **Phase E Chaos drill**：`POST /api/admin/ha/chaos/simulate-node-down`，仅非 production，production 硬拒 403 + 前端灰态
4. **Phase F 历史回放 UI**：`/admin/ha` Tab 表格 + drawer 明细 + 筛选
5. **Phase G 集成测试**：chaos + drift 混合场景，容器化 Incus fake cluster
6. **Phase H 验证 + runbook 文档**

### 验收标准

- [ ] Incus `/1.0/events` 事件流断线 3 次内自动重连 + 触发全量对账
- [ ] 关一台节点 → 30s 内 `healing_events` 出现 in_progress 行 → evacuate 完成后回填 evacuated_vms + status=completed
- [ ] 手动 evacuate 走新路径：先插 healing_events(trigger=manual, actor_id=admin) → 再调 Incus API
- [ ] Chaos drill 在 production 硬拒 403；staging 可用
- [ ] 前端 `/admin/ha` 历史 Tab 展示最近 50 条 + 筛选 + 明细 drawer
- [ ] 集成测试覆盖：drift / 事件断线重连 / 幂等 / chaos 全链路
- [ ] `docs/runbook-ha.md` 落地：事件流健康检查 / drift 告警处理 / chaos drill 使用

## 进行时描述

实现事件驱动 HA 与故障演练工具中

## 依赖

- **blocked by**: INFRA-006（PLAN-020 Phase A/B 必须先完成，event_listener 对接的是同一 reconciler 写入路径）
- **blocks**: 无

## 笔记

- 与 INFRA-006 共享 PLAN-020 文档；两者可并行开发，Phase C 开发时依赖 Phase A 的 reconciler 接口 ready
- 建议拆 PR：INFRA-006 先发（立即解决 drift 痛点），HA-001 随后
- 事件流 token 透传 / WebSocket 认证方式需 kickoff 前与运维确认
- Chaos drill 环境判定用 `ENV` 环境变量，不可被 header 伪造

### 2026-04-19 进度

- Phase C（事件流订阅）✅ 完成并部署生产（WebSocket over HTTP/1.1 ALPN、lifecycle 订阅、断线重连 + reconcileOnDemand）；踩了 2 个 Incus 版本坑（h2 拒绝 + type=cluster 不支持）
- Phase D（healing_events 手动 evacuate 双写）✅ 完成并部署生产（migration 008 已应用，Fail 路径 E2E 通过）
- Phase E（Chaos drill）✅ 完成并部署生产（env=production 默认硬拒 403，staging 切过去走 chaos 异步 goroutine）
- Phase D.2（cluster-member offline/online 自动建/关 healing auto 行）✅ 本地完成 + 单测 6 cases
- Phase D.3（instance-updated 追踪 AppendEvacuatedVM + ExpireStale 15min sweeper 接入 main）✅ 本地完成
- Phase F（/admin/ha Tabs 切换 + 历史事件表格 + 筛选 + Drawer 明细）✅ 本地完成（前后端 typecheck + build 全绿）
- INFRA-001 ✅ 随 PLAN-020 Phase A-F 关闭
- Phase H runbook 文档 ✅ `docs/runbook-ha.md` 完成（架构/健康检查/故障诊断/常见操作/告警建议/版本约束）
- Tabs 激活态 bug 热修 ✅（Base UI `aria-selected` vs Tailwind v4 `data-[selected]` 错配）
- Phase G 集成测试核心 ✅ — 3 组测试落地：`vm_reconciler_test.go`（7 case）+ `event_listener_test.go`（222 行 fake 接口层覆盖 D.2/D.3）+ `healing_test.go`（8 case 纯函数 serialise / parse）；全量 `go test ./internal/...` 全绿

### 2026-04-23 收官

Phase A-H 实用版全线交付。容器化 Incus fake cluster 的端到端 chaos 集成测试作为未来增量（工程成本高 ~5d，边际价值有限 — 现有 fake 接口层单测 + 生产多轮 E2E（含证书受限的 Fail 路径、staging chaos 异步、手动 evacuate 双写、浏览器 History drawer 渲染）已覆盖主要流程）。

HA-001 关闭；后续若需 fake cluster 回归测试，单开 task。

### 2026-04-24 正式 completed

用户提出关键洞察："测试环境本身就是真 Incus 集群（vmc.5ok.co 5 台物理机 + Ceph + /26 网段）" —— G.2 原定容器化 fake cluster 的 rationale（"用容器做高仿真"）因此消失，花 4-6d 搭低仿真模型去模拟现有高仿真真集群，ROI 倒挂。

正式动作：
- Phase G.2 关闭为 won't do（而非降级为烟雾脚本 —— 无立即需求）
- PLAN-020 切 completed，HA-001 切 completed
- 后续若"云端 Incus 测试环境"独立立项再起新 task，不挂 HA-001 包袱
