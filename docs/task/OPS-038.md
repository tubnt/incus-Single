# OPS-038 收尾包：5 项剩余优化全做

- **status**: completed
- **completedAt**: 2026-05-03
- **priority**: P2
- **owner**: claude
- **createdAt**: 2026-05-03
- **scope**: 用户 `这些都做了吧` —— 把"仍可做"清单 5 项全部落地

## 子任务（按由小到大顺序）

| # | 任务 | 估时 | 范围 |
|---|---|---|---|
| 1 | 路由 `as any` 收口（剩 8 处可改的部分） | 30min | console / index / sidebar / breadcrumb / command-palette / tooltip / checkbox |
| 2 | `firewall.tsx` rules 编辑器加 `_uiId` | 30min | 防"删中间行 input focus 跳"潜在 bug |
| 3 | 后端 enum 前端 i18n 映射 | 1.5h | VM status / Node status / HA message / 工单 / 订单 / 发票 |
| 4 | Long file split | 2h | `vm-detail.tsx` 622→482 / `users.tsx` 598→208 / `ha.tsx` 561→452 |
| 5 | CJK 字体自托管（修 Linux Chromium □ tofu） | 1h | 引入 `@fontsource-variable/noto-sans-sc`，挪进 `--font-sans` chain |

## 落地详情

### 子任务 1 — 路由 `as any` 收口
- `console.tsx` backUrl 改用字面量 union，去掉 2 处 Link `as any`。
- 余下 6 处运行期路径 / 第三方组件类型差异保留，但补齐 `eslint-disable` 注释和 `// runtime-typed` 说明，便于审阅。

### 子任务 2 — firewall `_uiId`
- `RulesEditor` rule state 内嵌 `_uiId: string`（`crypto.randomUUID()`）。
- `patch / remove` 按 `_uiId` 定位；`map` 渲染 `key={r._uiId}`；保存前 `({_uiId, ...r}) => r` 剥掉。

### 子任务 3 — enum i18n 映射
- 新增 `shared/lib/status-i18n.ts`：
  - `formatVmStatus / formatNodeStatus / formatNodeMessage / formatHaEnabled`
  - `formatTicketStatus / formatOrderStatus / formatInvoiceStatus`
- zh + en common.json 补 7 段 `*.statusValue.*`：
  - `vm.statusValue.{running,stopped,frozen,rescue,starting,stopping,error,pending,provisioning,gone}`
  - `admin.nodes.statusValue.{online,offline,evacuated,maintenance}` + `messageFullyOperational/messageClusterHealthy`
  - `ticket.statusValue.{open,answered,pending,closed}`
  - `order.statusValue.{pending,paid,provisioning,provisioned,active,expired,cancelled,refunded}`
  - `invoice.statusValue.{paid,open,void,draft,pending,overdue,cancelled}`
- 调用点：`vm-detail.tsx / vm-peek-panel.tsx / cluster-vms-table.tsx / admin/{ha,nodes,clusters,tickets,invoices,orders}.tsx / billing.tsx / tickets.tsx / invoice-detail-dialog.tsx / node-picker.tsx`。

### 子任务 4 — Long file split
- `users.tsx`：拆 `ShadowLoginDialog / QuotaEditor / UserRow` 到 `features/users/components/`。598 → 208。
- `vm-detail.tsx`：拆 `PortalFirewallPanel + FirewallGroupRow` 到 `features/vms/components/portal-firewall-panel.tsx`。622 → 482。
- `ha.tsx`：拆 `EventDetailDialog + DetailField` 到 `features/healing/components/event-detail-dialog.tsx`。561 → 452。

### 子任务 5 — CJK 字体自托管
- 新增依赖 `@fontsource-variable/noto-sans-sc@5.2.10`（OFL 1.1，按 unicode-range 分片懒加载）。
- `index.css` `@import "@fontsource-variable/noto-sans-sc/wght.css"`。
- `--font-sans` 串前置 `"Noto Sans SC Variable"`，置 Inter 之后、系统字体之前；保证 latin 仍走 Inter，中文走 Noto Sans SC，无系统字体环境（Linux Chromium）也不再 □。

## 验收

- [x] `bun run typecheck` PASS
- [x] `bun run lint --fix` 0 errors / 10 warnings（全部 pre-existing）
- [x] `bun run build` PASS · dist 同步 · `go build` PASS（23.6MB stripped binary）
- [x] 部署到生产 `incusadmin` 服务器：`incus-admin` sha256 `78a5f2d5c5e6...`，systemd active
- [x] 备份：`/usr/local/bin/incus-admin.bak.20260503_100231` / `…_112357`
- [x] PR review follow-up：`capitalize()` 抽到 `shared/lib/utils.ts`，删除 `ha.tsx` + `event-detail-dialog.tsx` 两处重复定义

## 回滚

```bash
mv /usr/local/bin/incus-admin.bak.20260503_100231 /usr/local/bin/incus-admin && systemctl restart incus-admin.service
```
