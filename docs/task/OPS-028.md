# OPS-028 全功能 UI 测试遗留打磨（CR + P3 + SEC-002）

- **status**: completed
- **completedAt**: 2026-04-30
- **priority**: P2
- **owner**: claude
- **createdAt**: 2026-04-30
- **plan**: [PLAN-030](../plan/PLAN-030.md)

## Description

OPS-027 全功能 UI 回归测试 + /pma-cr 收口。把 1 个 P2（SEC-002）+ 4 个 P3 + 3 个 CR 改进合一个 PR 收尾，避免多 PR churn。

## Acceptance criteria

- M1: `defaultProjectsFor` table-test 全绿
- L1: AddNode 拓扑覆盖字段 `safename` 校验生效（恶意输入死于 422）
- M2 / SEC-002: admin VM list / detail 响应不再含 `config.user.cloud-init`（password 不外泄）
- P3.1: billing.tsx 表头 `invoice.amount` / `invoice.status` 不再渲染 i18n key
- P3.2: admin/orders User/Product 列展示 email / 产品名（非数字 ID）
- P3.3: admin/tickets 用户列展示 email；EN locale 下页面标题 + 列名全英
- P3.4: env-script 下载点击触发 fetch + 401 step-up 拦截 → OIDC 跳转，不再落到裸 JSON
- L2: skip-network 高级区 IP placeholder 跟着 publicIP 末位走；未填 mgmtIP 时 warn 提示
- 后端 unit + 前端 tsc 全绿
