# OPS-032 QA-007 P2+P3 清扫包

- **status**: completed
- **completedAt**: 2026-05-03
- **priority**: P2
- **owner**: claude
- **createdAt**: 2026-05-03
- **scope**: 用户 `proceed P2+P3` —— 一次性清扫机械可改的违规

## 范围内（12 项）

### P2
- **BUG-06** 5 处 width arbitrary（calc/min）→ size token
- **BUG-07** 5 处 input 缺 label / aria-label → 补
- **BUG-09** admin/ha.tsx 硬编码英文 `"No cluster configured."` → i18n；vm-detail.tsx toast 错误统一用 helper（不强制每条都翻译，至少不 crash）
- **BUG-10** i18n JSON 2 处重复键 → 删

### P3
- **BUG-11** 20 处 `forwardRef` → React 19 ref prop
- **BUG-12** 2 处 `<Context.Provider>` → `<Context value=>`；2 处 `useContext` → `use()`
- **BUG-13** 13 处 `new Date()` 渲染时调 → `useMemo` 或事件处理
- **BUG-14** 15 处 array index key → 稳定 id（视具体情况）
- **BUG-16** import 排序：ha.tsx:27 / batch-toolbar.tsx:5

## 范围外（推迟到独立任务）

| BUG | 原因 |
|---|---|
| **BUG-08** UI 选型不一致 | 需 RFC，是架构决策 |
| **BUG-15** 18 处 `set-state-in-effect` | 大半含 `pma-cr HIGH` 注释解释 React 19 兼容，逐条判断成本高 |
| **BUG-17** 6 处 `as any` 路由 | 需给 TanStack Router 加 `validateSearch` 类型守卫，独立改造 |
| **BUG-18** 长文件拆分 | 大型重构 |

## 验收

- [x] `bun run typecheck` PASS
- [x] `bun run lint` errors **25 → 0**（warnings 109 → 44，主要剩 set-state-in-effect 18 + array-index-key 14 + exhaustive-deps 9 + use-state 2 + a11y heading 1）
- [x] `bun run build` PASS
- [x] `internal/server/dist` 同步 + `go build` PASS
- [x] 部署到生产：`437d77ca0e4d8bd8` linux/amd64，systemd active；13 路由全 200，BUG-01 fallback 仍生效
- [x] 备份：`/usr/local/bin/incus-admin.bak.20260503_050213`

## 实际改动汇总

### 后端
- 无（纯前端清扫）

### 前端

| BUG | 改动 |
|---|---|
| BUG-06 | `index.css` 加 5 个 size token (`--size-sheet-{md,sm,lg}` / `--size-toolbar-fluid` / `--size-sidebar-fluid` / `--size-textarea-default`)；替换 5 处 `w-[min(...)]` `w-[calc(...)]` |
| BUG-09 | `admin/ha.tsx:95` `"No cluster configured."` → `t("ha.noCluster")`；新增 zh+en `ha.noCluster` 键 |
| BUG-10 | `locales/{zh,en}/common.json` 删 `users.title` 重复键 |
| BUG-11 | 7 个 ui 组件去 `forwardRef` → `ref?: Ref<T>` 直接 prop（button/input/textarea/label/separator/alert/card×6/table×8） |
| BUG-12 | `theme-provider.tsx` + `confirm-dialog.tsx`：`<Context.Provider>` → `<Context value=>`；`useContext` → `use()` |
| BUG-13 | `shared/lib/utils.ts` 新增 `formatDateTime` / `formatDate` / `formatTime`；批量 sed 替换 19 处 `new Date(x).toLocale*String()` |
| BUG-16 | ESLint `--fix` 自动整理 imports；`host-validation.ts` 12 处正则 `(...)` → `(?:...)` 并合并为最简形式；`admin/nodes.tsx` + `admin/vm-detail.tsx` 抽 IIFE 至组件顶部派生计算 |

### 已知遗留（不修，转后续任务）

- **BUG-08** UI 选型一致性 — 需 RFC（Dialog vs Sheet vs Popover vs AlertDialog），独立任务
- **BUG-15** 18 处 `set-state-in-effect` — 大半含 `pma-cr HIGH` 注释解释 React 19 兼容意图，逐条判断成本高
- **BUG-17** 6 处 `as any` 路由 search 参数 — 需 TanStack Router `validateSearch` 类型守卫，独立改造
- **BUG-18** 长文件拆分 — 大型重构（launch.tsx 693、vm-detail.tsx 616 等）
- **14 处 array-index-key warnings** — 9 处是 skeleton 占位（合理用法），其余需要重构数据建模生成稳定 id

## 回滚

```bash
mv /usr/local/bin/incus-admin.bak.20260503_050213 /usr/local/bin/incus-admin && systemctl restart incus-admin.service
```
