# OPS-030 DESIGN.md 严格合规：清零 arbitrary value + console aria + api-tokens i18n

- **status**: completed
- **completedAt**: 2026-05-01
- **priority**: P3
- **owner**: claude
- **createdAt**: 2026-05-01
- **plan**: [PLAN-032](../plan/PLAN-032.md)

## Description

OPS-029 后做 DESIGN.md 视觉合规审查（颜色/字号/间距/圆角/阴影必须来自 Tailwind v4 @theme，禁止 arbitrary value 和 hex 字面量）。扫描发现 14 处 `[NNpx]` / `[Nrem]` 容器尺寸 arbitrary value + api-tokens.tsx 源码 hardcoded 中文（TTL_OPTIONS、formatTimeLeft）+ console.tsx 几个 aria-label 漏 i18n。统一清掉。

## Acceptance criteria

- `grep -E 'className=.*\[(#|rgba?\(|[0-9]+(px|em|rem))\]'` 返回 0 行
- @theme 暴露的 `--size-*` token 可在 Tailwind class 中按 `h-/w-/min-w-/max-w-/max-h-` 名字直接使用
- api-tokens 卡片不再渲染中文 fallback（"永不过期" / "1 小时" 等）
- console.tsx 顶栏 "返回" / "全屏" / "退出全屏" 走 t() i18n
- 后端 unit + 前端 tsc + build 全绿
- 生产 vmc.5ok.co 部署后 admin/monitoring chart / observability iframe / vm-detail console / 命令面板 / 各 dropdown 高度宽度无视觉退化
