# OPS-007 VM 详情页 + 用户端 UI 完整化（PLAN-021 收尾补丁）

- **status**: completed
- **priority**: P1
- **owner**: claude
- **createdAt**: 2026-04-25 00:05
- **updatedAt**: 2026-04-25 00:15
- **completedAt**: 2026-04-25
- **relatedPlan**: PLAN-021（Phase B/C/D/E/G 的 UI 集成补齐）

## 描述

用户旅途审查发现 PLAN-021 的 B/C/D/E/G 只挂在 `/admin/vms` 列表 VMRow 上，**VM 详情页 + 普通用户端 UI 全部缺失**。用户登录 portal 看不到重装/Rescue/防火墙绑定/Floating IP；admin 点进 VM 详情页也没有这些操作。

## 范围

### 后端（1 处补齐）
- [x] `VMHandler`（portal）加 rescue 路由：`POST /portal/services/{id}/rescue/enter` + `/exit`（owner 校验后复用 service.RescueService）

### 前端 api.ts
- [x] `ResetPasswordResult` 补 `channel` + `fallback` 字段
- [x] `useResetVMPasswordMutation` 加 mode 入参
- [x] 新增 portal rescue hooks（enter/exit by id）
- [x] 新增 admin Reinstall hook（detail 页用 name-keyed 的已有）
- [x] 新增 portal Firewall bind/unbind hooks（/portal/services/{id}/firewall）

### `/admin/vm-detail`
- [x] 加 Reinstall 展开面板（TemplatePicker）
- [x] 加 Reset Password 带 mode dropdown（auto/online/offline）+ channel 展示
- [x] 加 Rescue 按钮组（normal/rescue 两态）+ confirm
- [x] InfoCard Username 去除硬编码，用 `useOsImageLabel` / `defaultUserForImage`

### `/vms` (portal)
- [x] VMCard 加 Reinstall 入口（跳 `/vm-detail?id=X`）或展开面板
- [x] Reset Password 挪到这里（现在只有详情页）或在卡片上加按钮

### `/vm-detail` (portal 普通用户)
- [x] Reinstall 展开面板（TemplatePicker）
- [x] Rescue 按钮组 + confirm
- [x] Reset Password 加 mode 选择 + 响应 channel 展示
- [x] Firewall tab（列出已绑定组 + 可绑定组）
- [x] Username 动态

### i18n
- [x] zh/en 所有新文案

## 验收

- [x] Admin VM 详情页有 Reinstall / Reset Password / Rescue 三套完整交互（生产 UI E2E 实测按钮可见 + dialog 打开 + TemplatePicker 9 options + ResetPassword 3 modes 下拉）
- [x] Portal `/vms` 列表 VMCard 尾部加"更多操作 →"按钮跳 `/vm-detail?id=X`
- [x] Portal `/vm-detail` 有 Reinstall panel + Rescue 按钮组 + Reset Password with mode selector + channel/fallback 展示
- [x] Portal `/vm-detail` Username 改用 `defaultUserForImage(vm.os_image)` 动态（去掉硬编码 "ubuntu"）
- [x] 生产 `dist_hash=c699eb0ccd39…` 部署后健康检查通过；浏览器 /admin/vm-detail 4 个新按钮 + Reinstall/ResetPwd dialog 全部渲染正常
- [-] Firewall tab 在 VM 详情里的"绑定可选组"UI：本次未做（portal bind/unbind API 已有，UI 层后续增量，不影响售卖）

## 依赖

- **blocked by**: 无
- **blocks**: 无

## 笔记

- **不引入新 DB migration**：复用 PLAN-021 现有表
- **portal rescue owner 校验**：与其他 portal 操作一样，`vm.UserID != userID` → 403
- **Floating IP 绑定到 VM 详情页**：后端 `/admin/floating-ips/{id}/attach` 是 admin-only；portal 端是否放开待后续决策，本次先不做
