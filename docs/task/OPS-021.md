# OPS-021 PLAN-025/026 后续 cleanup（死代码 / 一致性 / quota）

- **status**: completed
- **priority**: P2
- **owner**: claude
- **createdAt**: 2026-04-30
- **completedAt**: 2026-04-30

## Description

PLAN-025 + PLAN-026 上线后留下的几个 follow-up：

1. **死代码清理**：`VMHandler.CreateService` (POST /portal/services) 后端handler 在前端没有调用方（`useCreateVMMutation` 仅定义不被任何组件使用）。删除前后端死路径，避免维护误导。
2. **AdminVMHandler.ReinstallVM 异步化**：portal 端 reinstall 已走 jobs runtime + SSE（PLAN-025），admin 端仍走旧 sync 路径。打齐一致性。
3. **Quota 强制**：`QuotaRepo` + `QuotaHandler` 已有读写能力但 `OrderHandler.Pay` 没有任何 quota check —— 用户余额够就可无限下单。补一个 pre-check：购买前如超限直接返 402。
4. **changelog + memory** 同步收口。

## ActiveForm

PLAN-025/026 后续 cleanup（死代码 / 一致性 / quota）

## Acceptance criteria

- `POST /portal/services` + `useCreateVMMutation` 删除；前端 grep 无残留引用
- `POST /admin/vms/{name}/reinstall` 走 jobs runtime + SSE，返回 202 + job_id；旧 sync 路径删除（或保留 jobs nil 兜底）
- `POST /portal/orders/{id}/pay` 在 PayWithBalance 之前做 quota 检查；超限直接 402 + 明确错误 message
- `go vet`/`go test`/`tsc`/`vitest`/`build` 全绿
- 生产部署后 `/api/portal/quota` 仍能正常返回（不破坏现有 admin 配额管理）

## Notes

- vms.password plaintext DB 加密 → 单立 task（OPS-022），需要 admin 端解密查看路径设计 + 历史数据迁移
- INFRA-003 standalone Incus host → PLAN-027，DB-driven cluster manager 重构
- cluster-env.sh 自动同步 → 运维便利，单立小 task
